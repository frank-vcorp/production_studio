# Checkpoint — IMPL-20260705-04 — Sandbox/Mock local Gemini

**Spec:** `SPEC-20260705-04_sandbox_mock_gemini.md`
**ID:** `ARCH-20260705-04` (delegación INTEGRA → SOFIA)
**Origen:** Usuario gastó ~$111 MXN en llamadas a Gemini API sin generar un clip funcional. Necesitamos sandbox determinista para validar flujos sin gastar créditos.

## Cambios

### Archivos NUEVOS (5)
- `src/services/sandbox/mockVision.ts` — Mock de Gemini Vision (VisualAnalysis determinista con hash por blob).
- `src/services/sandbox/mockVideo.ts` — Mock de Veo 3.1 (start + poll + extract; devuelve PNG con overlay SANDBOX).
- `src/services/sandbox/mockImageGen.ts` — Mock de Imagen 3 (PNG 512×512 con grid + sello SANDBOX).
- `src/services/sandbox/index.ts` — Re-exports centralizados (tree-shaking friendly).
- `src/utils/sandbox.ts` — Helper `IS_SANDBOX` + `SANDBOX_DISCLAIMER` (evaluado a nivel de módulo).
- `.env.example` — Plantilla documentada de `VITE_USE_SANDBOX`.

### Archivos MODIFICADOS (8)
- `src/services/gemini/imageAnalysis.ts` — Switch IS_SANDBOX al inicio de `analyzeImageForVision`.
- `src/services/gemini/video.ts` — Switch en `startVideoGeneration` / `pollVideoOperation` / `extractVideoFromOperation` (con nuevos params opcionales para sandbox). **RETRY_DELAYS_MS reducido de 5 a 2** (defensa en profundidad: costo máximo por fallo baja de ~$2.00 a ~$0.80 USD).
- `src/services/gemini/keyframeGenerator.ts` — Switch IS_SANDBOX al inicio de `generateKeyframeOut`.
- `src/types/keyframe.ts` — `VisualAnalysis.model` ahora acepta `'gemini-2.5-flash' | 'sandbox-vision-v1'`.
- `src/App.tsx` — Badge ámbar "🧪 SANDBOX — sin gastos de API" en el header cuando `IS_SANDBOX=true`. Tiene `data-testid="sandbox-badge"` para tests E2E.
- `src/components/prompt/PromptApprovalGate.tsx` — Modal de costo pre-generación (`showCostConfirm`). En sandbox salta directo a `doGenerate()`.
- `src/__tests__/setup.ts` — Polyfills jsdom: `OffscreenCanvas` (canvas 2D) y `Blob.arrayBuffer()`.
- `src/__tests__/geminiVideo.test.ts` — Test de `RETRY_DELAYS_MS` actualizado: ahora `length === 2`.

### Archivos NO versionados (gitignored, intencional)
- `.env.development` — `VITE_USE_SANDBOX=true` (dev local).
- `.env.production` — `VITE_USE_SANDBOX=false` (default para vite build local).

## Tests nuevos (11)
- `src/services/sandbox/__tests__/mockVision.test.ts` (3) — sandbox-vision-v1, determinismo por hash, shape completa.
- `src/services/sandbox/__tests__/mockVideo.test.ts` (3) — start/poll/extract con shapes correctas.
- `src/services/sandbox/__tests__/mockImageGen.test.ts` (2) — PNG válido + base64 decodificable.
- `src/services/sandbox/__tests__/toggle.test.ts` (3) — Toggle IS_SANDBOX=true/false verifica que geminiClient NO se llama cuando sandbox=true.

## Validaciones

| Check | Resultado |
|---|---|
| `tsc --noEmit -p tsconfig.app.json` | OK (exit 0) |
| `tsc --noEmit -p worker/tsconfig.json` | OK (exit 0) |
| `vitest run --reporter=basic` | **54 files / 313 tests passed** |
| `eslint` sobre archivos modificados | 0 errors, 1 warning pre-existente en `App.tsx:48` (no introducido por mí) |
| `vite build` | OK (5.96s, 331.73 kB index) |
| `.env.development` / `.env.production` en `.gitignore` | confirmado |

## Self-review manual (reemplazo de qodo, sunset)

1. **¿El mock es determinista?**
   - Vision: hash basado en `size:mime:primeros100bytes` del blob → mismo input = mismo output (verificado por test `mockVision.test.ts: mismo blob → mismo hash en subject`).
   - Video: operationId derivado de `Date.now()+random()` (único por ejecución, como en producción).
   - ImageGen: PNG generado con gradiente determinista basado solo en prompt.

2. **¿El badge SANDBOX es visible siempre que esté activo?**
   - Sí: el `IS_SANDBOX` se evalúa a nivel de módulo (no dinámico). El badge tiene `data-testid="sandbox-badge"` y está SIEMPRE renderizado en el header cuando `IS_SANDBOX=true`.

3. **¿El modal de costo solo aparece en producción?**
   - Sí: la condición `if (!IS_SANDBOX) { setShowCostConfirm(true); return; }` salta el modal cuando sandbox está activo.

4. **¿Los retries bajaron a 2?**
   - Sí: `RETRY_DELAYS_MS = [1000, 4000] as const` (verificado por test actualizado).
   - En sandbox el mock NO reintenta (es determinista).

5. **¿Hay regresiones posibles en el camino real (no-sandbox)?**
   - `extractVideoFromOperation` agregó params opcionales `fromKeyframe?`, `transition?` (no rompe callers existentes).
   - `generateTransition` ahora pasa estos params; en producción son ignorados (la ruta real solo usa `op.done` y `op.response`).
   - Los tests existentes de `geminiVideo.test.ts` (16 tests) siguen pasando sin cambios.

## Riesgos / Desviaciones

- **Mocks vs producción:** El "video" del sandbox es un PNG estático con overlay SANDBOX (NO un MP4 real). El reproductor lo aceptará pero el usuario debe entender que es simulación.
- **Tests jsdom:** Las stderr warnings `Not implemented: HTMLCanvasElement.prototype.getContext` son esperadas (jsdom no implementa canvas real). El polyfill agregado maneja la situación con fallback a PNG mínimo válido.
- **No afecta al Worker:** El proxy Cloudflare Worker no fue tocado (diff 100% cliente).

## Restricciones cumplidas

- NO se agregaron dependencias npm.
- NO se modificó el Worker.
- NO se pidió qodo (sunset). Self-review manual arriba.
- NO se commitearon `.env.*` (gitignored). Solo `.env.example`.
- `.gitignore` confirmado vía `git check-ignore`.

## Próximo paso (delegación a INTEGRA → GEMINI)

Si todo OK, INTEGRA confirma al usuario que es prudente recargar créditos antes de hacer tests E2E con la API real. El sandbox permite validar la UX completa (badge, modal de costo, overlays de carga) sin gastar API.