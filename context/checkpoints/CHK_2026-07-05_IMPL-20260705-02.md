# Checkpoint — IMPL-20260705-02

**Spec:** `SPEC-20260705-02_veo_body_fix.md`
**Commit:** `59ed255`
**Push:** `e853e7e..59ed255 main -> main` (https://github.com/frank-vcorp/production_studio)
**Vercel:** HTTP 200 post-deploy
**Smoke Veo endpoint:** HTTP 200 → `{"name":"models/veo-3.1-generate-preview/operations/d0psud2x9vep"}`

## Cambios (3 archivos, +120 / -16)

### `src/services/gemini/video.ts`
- `startVideoGeneration`: `image.data` → `image.bytesBase64Encoded`; eliminado `personGeneration: 'dont_allow'`.
- `classifyVeoError`: nueva cascada de extracción de mensaje (`e.message` → `e.body.error.message` → `e.body` string → `e.details.error.message` → `e.details.message` → `err` string → `JSON.stringify` truncado a 500 chars). Filtra `"[object Object]"` literal.

### `src/types/gemini.ts`
- `GenerateVideoRequest.instances[].image`: `data` → `bytesBase64Encoded`.
- `GenerateVideoRequest.parameters`: `personGeneration` removido (con comentario explicando el porqué).

### `src/__tests__/geminiVideo.test.ts`
- Test body shape actualizado: verifica `bytesBase64Encoded`, ausencia de `data` y de `personGeneration`.
- 4 tests nuevos para `classifyVeoError`:
  1. `body: {error: {message: "..."}}` → extrae mensaje (no `[object Object]`).
  2. `body: "Internal Server Error"` (string) → mensaje literal.
  3. `details: {error: {message: "..."}}` (formato `GeminiProxyError` real) → extrae mensaje.
  4. `message: "[object Object]"` literal + body válido → prioriza body.
  5. Body con "safety" en mensaje → clasifica como `safety` no retryable.

## Validaciones

| Check | Resultado |
|---|---|
| `pnpm typecheck` (app + worker) | OK |
| `pnpm test --run --reporter=basic` | 49 files / 290 tests passed |
| Lint diff | OK en mis archivos (2 errores pre-existentes en `src/App.tsx`, ajenos) |
| Vercel `production-studio-gamma.vercel.app` | 200 |
| Worker `generateVideo` con body nuevo | 200 OK |
| Worker redeploy | **NO requerido** — diff 100% cliente |

## Self-review manual (reemplazo de qodo, sunset)

- ¿El código refleja la SPEC? Sí. Los 3 issues están resueltos en orden.
- ¿Code smells?
  - La cascada de `classifyVeoError` es larga pero cada rama es simple y testeada.
  - Consideré extraer un helper `extractErrorMessage(err)` pero el árbol solo se usa aquí; YAGNI.
  - El type cast de `e.body.error` a `string | { message?: string }` podría simplificarse con un type guard, pero la verificación `typeof` + `'message' in errField` ya cubre el caso. Trade-off aceptable.
- ¿Tests cubren edge cases de la SPEC?
  - `body` objeto con `error.message` ✓
  - `body` con `error` string ✓
  - `body` string puro ✓
  - `details` con `error.message` (camino real de `GeminiProxyError`) ✓
  - `"[object Object]"` literal como message (defensa en profundidad) ✓
  - Sin `body`/`details` (caso string de `err`) ✓
  - Caso safety classification con body ✓
- ¿Riesgo de regresión?
  - Imagen (`keyframeGenerator.ts`) sigue usando `personGeneration: 'dont_allow'` y NO fue tocado (la API de Imagen sí lo soporta).
  - Los 5 tests originales de `classifyVeoError` siguen pasando (compatibilidad con `e.message` string intacta).
  - `e.details` ahora también se conserva en el `VeoError` resultante (fallback a `e.body` si no hay `details`), preservando telemetría.

## Worker — NO modificado

Confirmado por el smoke test: el Worker sigue aceptando el body nuevo porque solo hace forward crudo al API de Gemini. La forma del JSON que recibe es exactamente la que el API de Veo espera.

## Próximos pasos (mano del usuario)

1. Probar manualmente desde el frontend: aprobar transición → debería arrancar Veo con operation name real, no 400.
2. Si el toast de error reaparece con mensaje legible (no `[object Object]`), cerrar como válido.
3. Considerar v1.1: moderation check pre-envío si preocupa generación de personas reales (ahora el default de la API permite todo).